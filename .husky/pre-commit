#!/bin/sh
# Pre-commit hook — runs on every `git commit`
# Blocks commit if secrets are detected in staged files

# 1. Secret scan (blocks if gitleaks finds anything)
gitleaks protect --staged --redact --config .gitleaks.toml --verbose

# 2. Format + lint staged files (eslint --fix → prettier --write)
npx lint-staged
