{
  "name": "notebookzen",
  "private": true,
  "version": "1.0.0",
  "description": "NotebookZen — monorepo (backend + frontend + infra)",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.2.0"
  }
}
